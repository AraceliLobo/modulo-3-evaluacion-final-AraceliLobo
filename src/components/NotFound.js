import React from "react";
import "../stylesheets/layouts/NotFound.scss";

const NotFound = () => {
  return <p className="not-found">Sorry, we have not found the character</p>;
};

export default NotFound;
